apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "oci-quickcache.fullname" . }}
  labels:
    {{- include "oci-quickcache.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "oci-quickcache.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "oci-quickcache.fullname" . }}
    namespace: {{ .Release.Namespace }}
